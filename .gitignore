# 官方參考 repo（389MB，含二進位韌體與原理圖 PDF），不進版控
reference/

# ESP-IDF
build/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock

# PlatformIO
.pio/
.pioenvs/
.piolibdeps/

# Arduino IDE
*.bin
*.elf
*.map

# 編輯器 / OS
.vscode/
.idea/
.DS_Store
*.swp

# Python venv（PlatformIO CLI）
.venv/

# lv_font_conv 的 Node 相依（產生的字型 .c 本身有進版控）
tools/node_modules/
tools/package-lock.json
tools/package.json

# OpenSCAD 產出的 STL。原始碼是 cad/*.scad，STL 隨時可以重新 render。
cad/out/

# Meshy 產的概念圖，只是設計參考，不是可列印模型
cad/concepts/

# 本機專屬的 PlatformIO 設定（燒錄埠等）。範本是 platformio_local.ini.example。
platformio_local.ini

# AI 工具的 skill 設定，與韌體無關
.agents/

# 別人的模型檔（參考用，不是我們的作品，不進版控）
cad/*.3mf
